# 完整项目依赖：本地模型加载 + API 客户端 + Web 控制器。
# 安装：python -m pip install -r requirements.txt
# vLLM 服务端使用 WSL 的独立 vllm-env，不在这里安装。

# API 客户端与输出校验
openai>=1.0,<3
pydantic==2.13.5

# 模型推理
torch==2.14.0
transformers==5.16.1
accelerate==1.14.0

# JSON Schema 约束解码与输出校验
outlines[transformers]==1.3.3

# HTTP 服务
fastapi==0.141.1
uvicorn[standard]==0.52.4

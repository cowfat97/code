# Qwen3 本地推理依赖（2026-09-07 验证，llm 环境可跑通 quick_start.py）
# Python 3.11.15（uv 创建；requirements.txt 不锁 Python 版本，由 venv 决定）
# 安装：uv pip install -r requirements.txt

torch==2.14.0
transformers==5.16.1
accelerate==1.14.0
